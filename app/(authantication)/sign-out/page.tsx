import React from 'react'

const SignOut = ({children}:{children: React.ReactNode}) => {
    return (
        <div>
            <h1>Sign-out</h1>
            {children}
        </div>
    )
}
export default SignOut
